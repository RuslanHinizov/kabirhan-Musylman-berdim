# Race Vision Configuration

# RTSP default URL (used when no camera-specific URL is set)
RTSP_DEFAULT_URL=rtsp://admin:Zxcv2Zxcv2@10.223.70.32:554//stream

# Server
SERVER_HOST=0.0.0.0
SERVER_PORT=8000

# GPU acceleration (overridden by --gpu CLI flag)
USE_GPU=true

# Model paths (relative to project root)
# TensorRT requires tensorrt Python package — use .pt if not installed
MODEL_PATH_YOLO=yolov8s.engine
MODEL_PATH_COLOR=models/color_classifier.pt

# Detection
DETECTION_INTERVAL=0.10
DETECTION_MODE=internal
# Max cameras processed per detection cycle (increase with TensorRT)
# PyTorch: 5, TensorRT: 8-10
MAX_CAMERAS_PER_CYCLE=8

# Camera counts
NUM_ANALYTICS_CAMERAS=25
NUM_PTZ_CAMERAS=4

# Track
TRACK_LENGTH=2500

# Logging
LOG_LEVEL=INFO
